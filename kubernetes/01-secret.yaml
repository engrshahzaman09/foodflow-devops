# Template only — do NOT commit real secrets to Git.
# Create the actual secret on your cluster with:
#   kubectl create secret generic foodflow-secrets \
#     --namespace=foodflow \
#     --from-literal=MYSQL_ROOT_PASSWORD=<your-password> \
#     --from-literal=JWT_SECRET=<your-jwt-secret>
apiVersion: v1
kind: Secret
metadata:
  name: foodflow-secrets
  namespace: foodflow
type: Opaque
stringData:
  MYSQL_ROOT_PASSWORD: "changeme"
  JWT_SECRET: "changeme"
