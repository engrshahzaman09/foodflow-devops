apiVersion: apps/v1
kind: Deployment
metadata:
  name: foodflow-app
  namespace: foodflow
spec:
  replicas: 2
  selector:
    matchLabels:
      app: foodflow-app
  template:
    metadata:
      labels:
        app: foodflow-app
    spec:
      containers:
        - name: foodflow-app
          image: engrshahzaman09/foodflow:latest
          ports:
            - containerPort: 8087
          env:
            - name: SPRING_DATASOURCE_URL
              value: jdbc:mysql://foodflow-mysql:3306/foodflow
            - name: SPRING_DATASOURCE_USERNAME
              value: root
            - name: SPRING_DATASOURCE_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: foodflow-secrets
                  key: MYSQL_ROOT_PASSWORD
            - name: JWT_SECRET
              valueFrom:
                secretKeyRef:
                  name: foodflow-secrets
                  key: JWT_SECRET
          readinessProbe:
            httpGet:
              path: /actuator/health
              port: 8087
            initialDelaySeconds: 30
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /actuator/health
              port: 8087
            initialDelaySeconds: 45
            periodSeconds: 15
---
apiVersion: v1
kind: Service
metadata:
  name: foodflow-app
  namespace: foodflow
spec:
  selector:
    app: foodflow-app
  type: LoadBalancer
  ports:
    - port: 80
      targetPort: 8087
